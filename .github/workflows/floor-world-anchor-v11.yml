name: World Anchored Floor V11
on:
  push:
    paths:
      - '.github/workflows/floor-world-anchor-v11.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: python tools/floor_world_anchor_v11.py
      - run: |
          grep -q 'WORLD-ANCHORED FLOOR' index.html
          grep -q 'const tile=180' index.html
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git commit -m 'Anchor realistic stone floor to world coordinates'
          git push